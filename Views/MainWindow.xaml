<Window x:Class="FocusBuddy.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FocusBuddy"
        Height="760"
        Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#252525" Padding="16">
            <StackPanel>
                <TextBlock Text="FocusBuddy" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,18"/>
                <TextBlock Text="Personal usage analyzer" Margin="0,0,0,30" Foreground="#BBBBBB"/>

                <Button Content="Refresh Dashboard" Command="{Binding RefreshDashboardCommand}" />
                <Separator Margin="0,14" />
                <TextBlock Text="Focus Mode" FontSize="18" Margin="0,0,0,8" />
                <CheckBox IsChecked="{Binding IsFocusModeEnabled}" Content="Enable Focus Mode" Margin="0,4" Foreground="White"/>
                <CheckBox IsChecked="{Binding AutoMinimizeDistractingApps}" Content="Auto-minimize distracting apps" Margin="0,4" Foreground="White"/>
                <TextBlock Text="Blacklist (one process per line):" Margin="0,12,0,6"/>
                <TextBox Text="{Binding BlacklistRaw}" AcceptsReturn="True" Height="170" Background="#333" Foreground="White" BorderThickness="0"/>
                <Button Content="Save Focus Settings" Command="{Binding SaveFocusSettingsCommand}" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <Border Background="#2A2A2A" CornerRadius="8" Padding="16" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Text="Today Total" FontSize="16" Foreground="#AFAFAF"/>
                        <TextBlock Text="{Binding Dashboard.TodayTotalTime}" FontSize="30" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#2A2A2A" CornerRadius="8" Padding="12" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="Today by Category" FontSize="18" Margin="0,0,0,12"/>
                            <ItemsControl ItemsSource="{Binding Dashboard.CategoryBreakdown}" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="#2A2A2A" CornerRadius="8" Padding="12" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Last 7 Days" FontSize="18" Margin="0,0,0,12"/>
                            <ItemsControl ItemsSource="{Binding Dashboard.WeeklyTotals}" />
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Background="#2A2A2A" CornerRadius="8" Padding="12" Margin="0,14,0,0">
                    <StackPanel>
                        <TextBlock Text="Top 5 Apps Today" FontSize="18" Margin="0,0,0,8"/>
                        <ItemsControl ItemsSource="{Binding Dashboard.TopApps}" />
                    </StackPanel>
                </Border>

                <Border Background="#2A2A2A" CornerRadius="8" Padding="12" Margin="0,14,0,0">
                    <StackPanel>
                        <TextBlock Text="Category Rules" FontSize="18" Margin="0,0,0,8"/>
                        <ItemsControl ItemsSource="{Binding CategoryRules}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Category}" Margin="0,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
